HTML is used to build the basic structure of the web page.

CSS is used to style the page — colors, fonts, layout, and responsiveness.

JavaScript adds interactivity — like button clicks, form actions, or dynamic updates.
